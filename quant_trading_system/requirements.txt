# 퀀트 트레이딩 시스템 의존성
# ============================================================
# 필수 패키지
# ============================================================
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
yfinance>=0.2.36
pyarrow>=12.0.0           # Parquet 캐시

# ============================================================
# 딥러닝 — CPU 버전 (기본)
# PyTorch GPU 설치 시 아래 주석 참고
# ============================================================
torch>=2.1.0
torchvision>=0.16.0

# PyTorch GPU (CUDA 12.1):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ============================================================
# 시각화
# ============================================================
matplotlib>=3.7.0
Pillow>=9.0.0

# ============================================================
# GPU 모니터링
# ============================================================
pynvml>=11.0.0            # NVIDIA GPU 모니터 (CUDA 드라이버 필요)

# ============================================================
# 선택적 패키지 (기능 확장)
# ============================================================
# xgboost>=2.0.0          # GBM 앙상블
# lightgbm>=4.0.0         # GBM 앙상블
# shap>=0.42.0            # 피처 중요도
# optuna>=3.4.0           # 하이퍼파라미터 최적화
