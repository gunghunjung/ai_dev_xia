# ══════════════════════════════════════════════════════════════
# requirements.txt — 오프라인 문서 변경 분석 AI
# 아나콘다(Anaconda) 환경 기준
# 설치: pip install -r requirements.txt --no-index --find-links ./wheels/
# 온라인 설치: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# ── 핵심 딥러닝 ──────────────────────────────────────────────
torch>=2.0.0,<3.0.0
# CPU only 환경 (GPU 없음): pip install torch --index-url https://download.pytorch.org/whl/cpu
# GPU 환경: conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia

# ── HuggingFace Transformers (오프라인 핵심) ─────────────────
transformers>=4.35.0,<5.0.0
tokenizers>=0.15.0
sentencepiece>=0.1.99          # T5 / mT5 토크나이저 필수
protobuf>=3.20.0               # sentencepiece 의존성

# ── 데이터 처리 ──────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0                # .xlsx 읽기/쓰기 필수

# ── 머신러닝 유틸 ─────────────────────────────────────────────
scikit-learn>=1.3.0            # LabelEncoder, train_test_split, TF-IDF

# ── 진행률 표시 ──────────────────────────────────────────────
tqdm>=4.66.0

# ── 선택: 가속 (CPU 환경에서도 동작) ─────────────────────────
# accelerate>=0.24.0           # 멀티 GPU / 대형 모델 학습 시 활성화

# ── 선택: 양자화 (CPU 경량화, Intel 환경) ────────────────────
# intel-extension-for-pytorch  # Intel CPU 최적화 시 활성화

# ── 아나콘다 기본 포함 (별도 설치 불필요) ────────────────────
# python>=3.9
# pickle (표준 라이브러리)
# json   (표준 라이브러리)
# argparse (표준 라이브러리)
# logging  (표준 라이브러리)
# os, sys, re, datetime (표준 라이브러리)
